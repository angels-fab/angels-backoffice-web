# Bridge State

## Current Goal

- Keep Claude Code and Codex synchronized through repository files.
- Use this file as the compact shared memory for the current work stream.
- Enforce the agreed role split (기준: 루트 `AGENTS.md`): Claude Code implements and syncs git; **Codex = 사용자용 UX/UI/기능 자문위원**(요약·의미설명·선택지·추천). Codex는 사용자가 명시 요청하지 않는 한 Claude용 프롬프트를 작성하지 않음.

## Current Focus

- **5개 보완: 캘린더필터·간트편집·홈로드맵폭·개선요청인라인(2026-07-01)**: **프런트 전용**(Code.gs 무변경 → clasp 불필요). type-check+build 통과, 콘솔0. ① **업무일정 필터**(`Calendar/index.tsx`+`CalFilterBar.tsx`): 종류 '전체'칩 제거. 종류/팀원 일반클릭=단일, **단일선택 재클릭=해제(종류→전체표시 / 팀원→전체선택[])**, Shift+클릭=추가토글. 종류는 현재기간·팀원·검색 기준 **0건 탭 숨김**(catCounts), 선택된 종류가 0건되면 `useLayoutEffect`로 **자동 해제**. **복수선택 버튼 PC 제거·모바일만**(`showMulti={isMobile}`, useMediaQuery 768). 칩 `user-select:none`+mousedown shift preventDefault(텍스트선택 방지). 실측(PC): 단일/재클릭/Shift/멤버재클릭·복수버튼 숨김·userSelect none·0건 숨김. ② **장비 간트 편집 저장구조 전면 개편**(`Equipment/index.tsx`): 드래그/리사이즈/✓는 **로컬 Redux만**(persistBatch 제거). 편집시작 `snapshotRef` 보관→**'편집 종료'→저장/변경취소/계속편집 다이얼로그**. 저장=변경분(snapshot diff)만 **1회 일괄 persistBatch**+reload / 변경취소=snapshot으로 setScheduleStart·setScheduleStage 복구 / 변경없으면 시트 호출 없음. **개별 확인 UI를 중앙 모달→포인터근처 소형(`PendingConfirm` portal, ✓반영/✗취소)**. undo/redo도 로컬 전용. 드래그 안내 **3줄**(단계명+변경량 / 전→후 / 변경 취소: ESC). 실측: ✓시 POST 0건·저장시 POST 1건·3줄툴팁·포인터확인·편집종료 다이얼로그. ③ **홈 로드맵 폭**(`Home/RoadmapCard.tsx`): `maxWidth:940·mx:auto` 제거→`width:100%` → 아래 섹션 카드와 좌우 정렬(폭제한은 PageContainer). ④ **개선요청 인라인 복원**(`Improve/index.tsx`): Phase2 **모달→표 상단 인라인 작성 복원**(renderComposeCard). '새 요청'=인라인 토글, 요청추가·임시저장·N건등록·취소 컨트롤행. **배경클릭 닫힘 없음**. **닫기경고=텍스트유무 아닌 마지막저장본(`baselineRef` 직렬화) 대비 실제 변경분**(빈카드추가·저장후무변경·저장본동일=경고없이 닫힘 / 저장후 재수정=경고). 임시저장 성공시 baseline 갱신·실패시 미갱신. 등록성공시 doClose(초기화). ⑤ **페이지메모 guest 숨김**: 이미 `admin=isAdmin&&user&&authKey` 게이트로 chip/panel/snackbar 전부 null(무변경, 확인만). 실측: 인라인(모달아님)·요청추가2·빈취소 즉시닫힘·dirty취소 3택확인.
- **업무현황 진행중 카드 삽입정렬 드래그 순서변경(2026-07-01)**: 시안 `docs/mockups/work-card-reorder.html` 기준. ① **드래그 UI**(신규 `src/pages/Work/ReorderableTaskGrid.tsx`): 핸들 없이 카드 전체 잡아 이동(버튼·링크 `closest('button,a')` 제외), **삽입정렬**(A→D자리=B-C-D-A, 원본 slotRects 1회 캡처·매 이동 재계산=누적오차 없음), 목적지 점선 placeholder + 영향 카드 FLIP(useLayoutEffect, MOVE 180ms), 들어올린 카드 position:fixed 오버레이(ref로 명령형 위치·리렌더 안전). 마우스 8px / **터치 250ms 롱프레스**(이동 먼저=스크롤로 취소, touchAction pan-y), 짧은클릭=열기(suppressClickUntil 350ms), **Esc/취소=원위치**(finishDrag(false)). **hover 억제**: dragNum 동안 그리드 `& .reorder-cell{pointer-events:none}` → 드래그·다른 카드 hover 테두리 원천 차단(placeholder 점선 유지), 종료 시 자동 복원. ② **정렬/저장**(`Work/index.tsx`): `inProgressList`=포털정렬순서(낙관적 `orderMap` 우선)→cmpChief. `handleReorder`=낙관적 orderMap + 5s 디바운스, `flushOrderSave`=**동시저장 방지 savingRef + 성공(비행중 변경없음)시에만 pending 클리어 + finally서 최신 순서 이어서 저장**. 성공 무표시·**실패만 오류 Snackbar+다시시도**. pagehide/visibilitychange(hidden)/언마운트 `beaconWorkOrder`(sendBeacon, 큐잉 성공시에만 pending 클리어). ③ **백엔드**(`Code.gs`): `workCtx_` C.order(`포털정렬순서/정렬순서/표시순서/순서`), `getWorks_` order 반환, `updateWorkOrder_`(포털정렬순서 열만 갱신·행이동 없음·변경분만·**열 없으면 헤더 끝에 자동생성**), doPost work 락그룹에 라우팅. `api/sheets.ts` `updateWorkOrder`/`beaconWorkOrder`(bool)·WorkRow/WorkItem order. **프런트+백엔드**, type-check+build 통과. 라이브 dev 실측(admin 임시키): 마우스 드래그 A→slot3=B-C-D-A, midCell N-1·placeholder·pointerEvents none→auto, Esc 복원, 클릭 선택·드롭후 클릭억제, 더보기 메뉴 동작, **터치 롱프레스 250ms 후 드래그**. **clasp @58→@59 재배포 완료**(updateWorkOrder 라우팅·getWorks order 확인). 적대적 리뷰 5렌즈·15에이전트: 확정 4건(전부 save-orchestration)=수정(동시저장 savingRef·비행중 pending보존·beacon 실패시 유지). Esc애니메이션 미적용=시안차이(스펙은 기능복원만 요구, ship). **잔여(사용자)**: 실 로그인 E2E(드래그→5s후 저장→새로고침 유지, 여러번 이동시 마지막만). 포털정렬순서 열은 첫 저장 시 자동 생성됨.
- **Phase2 백엔드 3항목 — 임시저장·일괄등록·작성모달(2026-07-01)**: Phase1의 남은 #8/#9/#12 구현·배포. ① **#9 수동 임시저장**: 새 시트 `포털개선요청_임시저장`(사용자 세팅: 배너 2행+헤더 3행 `임시저장ID·긴급·제목·관련자료·개선위치·작성자·최종저장일시·요청내용`). `Code.gs` `draftCtx_`(replyCtx_ 패턴, 헤더 자동탐지 첫10행·행위치 비의존, 없으면 생성)·`getDrafts_`(본인 것만·authError_)·`saveDrafts_`(본인 것 **전체 대치**)·`deleteDrafts_`(ids 또는 본인 전체). 자동저장 없음. ② **#12 일괄등록**: `createImprovements_`(각 카드=독립 게시글, 단건과 동일 규칙 상태접수·제안일자오늘·담당자=작성자·유형''·메모FALSE, 번호 순차부여, copyRowValidation_/체크박스). doPost 라우팅 추가(getDrafts=읽기/무락, save·delete·createImprovements=LockService). ③ **프런트**: `api/sheets.ts` `fetchDrafts/saveDrafts/deleteDrafts/createImprovements`(+DraftRow/DraftInput, postDraft 미배포시 friendly error, createImprovements 재시도 안함=중복등록 방지). `Improve/index.tsx` 신규작성을 **멀티카드 Dialog**로 전환(인라인 수정은 renderEditRow로 유지): 카드별 긴급·제목·개선위치·관련자료·요청내용, 요청추가(개선위치 미승계)·카드삭제, 하단 임시저장/취소/N건등록. **#8 배경클릭·Esc 닫힘 비활성**(MUI v9=disableEscapeKeyDown 제거됨 → controlled Dialog onClose no-op로 구현), X/취소만 requestClose→내용있으면 3택 확인(계속작성/저장안함/임시저장후닫기). 중복제출 가드(savingDraft/publishing). **적대적 리뷰 5렌즈·18에이전트: 확정 3건(전부 데이터유실)=수정**: (a) `saveDrafts_` 빈 payload여도 선삭제→기존 임시저장 유실 → 저장할 카드 없으면 삭제 skip+기존 반환. (b) `handlePublish` 게시 후 deleteDrafts를 ids없이 호출=본인 전체삭제(미게시 저장분까지) → 게시한 카드 id만 삭제. (c) 클라 빈저장 wipe → handleSaveDrafts payload 비면 서버 미호출. 나머지(재시도 latency·헤더행 위치 등) refuted. type-check+build 통과. **clasp 재배포 @57→@58 완료**(동일 /exec, `deploy -i`), 라이브 4액션 라우팅 확인(bogus creds→auth error). **잔여(사용자)**: 실제 로그인(담당자 비번)으로 E2E(임시저장 저장·불러오기·일괄등록·확인팝업).
- **Phase1 개편 14항목 中 프런트 10항목(2026-07-01)**: 사용자 14항목 요청을 2단계로 분리 — 이번은 **프런트 전용 10항목**(#8/#9/#12 백엔드는 Phase2). ① **장비관리 관리자전용**(#1): `AppRouter` `/equipment`·`/equipment-ops`를 `RequireAdmin`으로 감쌈(!isAdmin→`/`) · `SideNav.GUEST_PATHS`={`/`,`/events`,`/links`}(장비 제외) · `BottomNav` 장비 isAdmin게이트 · `MainLayout` `loadEqData()`도 isAdmin일때만 dispatch(게스트 API차단) · `Home` 장비섹션 isAdmin블록. 로그인/로그아웃 즉시반영(useRole context). ② **캘린더 필터**(#2, `Calendar/index.tsx`+`CalFilterBar.tsx`): 일반클릭=단일선택 / Shift·모바일복수모드=추가토글(`additive=e.shiftKey||multi`). 종류 맨앞 '전체'칩(`selectAllCats`→`setSelCats([])`), 전체↔개별 상호배타(뷰파생 `allCatsOn=selCats.length===0`), 모두해제→전체복귀. 팀원/종류 독립그룹. `catCounts`는 종류선택 미반영(선택가능수 유지). ③ **장비도입 보기순서/기본**(#3): 단계별→타임라인→목록, 기본=`stage`. ④ **간트 리사이즈 실시간+터치**(#4): drag/resize/firstResize를 Pointer Events화(setPointerCapture+window리스너+touchAction:none), onMove서 `itemTimelineForMonths` 재계산해 라이브 미리보기, Esc복원(`abortedRef`), 저장은 드래그종료 후. `measureHalfPx` 실측셀폭. ⑤ **첫 단계 시작핸들**(#5, `gantt.tsx` 좌핸들 `.gantt-resize-start`): 첫단계만 늘고/줄고 끝 고정(setScheduleStart+setScheduleStage 재사용), 타임라인 시작년월 자동. ⑥ **행사 2탭**(#6, `Events/index.tsx`+신규 `EndedList.tsx`): 진행·예정(green→amber start asc)=카드/캐러셀 · 종료(gray end desc)=밀도목록(행사명/구분/기간/장소, 행클릭 상세 Dialog=EventCardInner 재사용) · 탭별건수. `MobileCarousel` 내부3탭 제거→평면레일. PC/모바일 공통 2탭. ⑦ **로드맵 제거→홈이식**(#7): `/roadmap` 라우트·SideNav항목·`pages/Roadmap/`·`Home/RoadmapTimeline.tsx` 삭제, 신규 `Home/RoadmapCard.tsx`(자립)를 홈 주요섹션으로(게스트도 노출). `improveMemo` '구축 로드맵'→`/`. ⑧ **페이지메모 상태변경**(#10, `PageImprovementMemo.tsx`): 관리자=상태 Select(메인과 동일 값/색/권한 improveMeta), isSettled(보류·완료·불가) 확인팝업(needsReason만 사유필드), 그 외 즉시, `updateImprovement`+reload. 게스트=StatusChip 읽기전용. ⑨ **유형 제거**(#11, `Improve/index.tsx`): 작성/수정/상세/목록/검색에서 유형 삭제(cType·payload·DropField·헤더·행셀·typeOptions·IMP_TYPE_OPTIONS), colSpan 재계산. 개선위치(loc) 필수유지. 시트 type컬럼은 호환 위해 유지하되 미표시(create가 미전송→'' 기록). **프런트 전용**(백엔드/clasp/시트 무변경). type-check+build 통과, 콘솔 신규에러0(잔존은 편집중 스테일 `?t=` — 풀리로드 후 전 라우트 정상렌더). 적대적 리뷰 7렌즈·11에이전트: **확정 1건**=페이지메모 완료전환이 확인팝업 vs 메인보드 완료 즉시(불일치) → **판단: 페이지메모는 사용자 #10 명시("보류·완료·불가 확인팝업")에 정확히 부합하므로 수정 안 함**, 메인보드 통일 여부는 사용자 결정사항으로 보고. 나머지 6렌즈 clean. **Phase2(백엔드, 별도·clasp 재배포 필요)**: #8 배경클릭닫힘 비활성+종료확인 / #9 수동 임시저장(새 시트 `포털개선요청_임시저장`) / #12 멀티카드 작성모달+일괄등록. **커밋·자동배포 진행.**
- **업무일정 주간 겹침 + 장비 타임라인/목록/누락 보완(2026-06-30 #2)**: ① **주간 겹침**: `Calendar` FullCalendar에 `slotEventOverlap={false}` — 같은 시간 일정이 50% 겹침→좌우 균등분할(겹침0). 월간·호버/클릭(elementsFromPoint 위임)·손가락포인터·종류건수 무영향. 실측: 동시 3건 → 3등분 컬럼 overlaps0, 호버/cursor pointer OK. ② **타임라인 수량**: 배지 폐지, 1대=미표시·2대+=장비명 옆 강조텍스트(`batchUtil.NameWithQty`, 0.82em). 타임라인·단계카드·목록·대장 공통. ③ **타임라인 밀도**: 이름 아래 단계/담당/관리번호 보조줄 제거(상세 드로어에만), 행 minHeight 44→32·이름열 220→150. ④ **가로스크롤 제거**: 간트 `repeat(n, minmax(0,1fr))`(연도행도 fr)·overflow-x 제거·휠→가로변환 핸들러 삭제. 드래그/리사이즈 스냅은 `measureHalfPx`(실측 셀폭=간트영역/(월*2))로 — 가변폭에서도 정확(실측 21월·42px/월, +1개월 드래그=2028.7→2028.8). 오늘선=`calc(150px + frac*(100%-150px))`. PC 가로스크롤0·상하스크롤 좌우샘 없음, 모바일도 fit(가로스크롤0). ⑤ **도입예정 누락 제외**: `batchUtil.isRegRequired`(도입중·운영중·비가동만 필수)·`missingLabels`가 데이터 로직 자체에서 도입예정/미분류는 []. 운영 요약(필수정보 누락 20종·29대→0종·0대)·대장 누락열('—')·설치장소색·운영 드로어 3상태박스(도입예정=중립)·도입 드로어 등록정보(누락/황색 미표기) 일관. ⑥ **목록 정렬**(`sortable.tsx`: useTableSort/sortRows/SortTh): 도입목록+운영대장 헤더 클릭 asc→desc(MUI Arrow아이콘), 숫자=숫자·날짜(dueMonth 'YYYY.M')=값·한글=localeCompare, 빈값 항상 끝, filtered/listed(검색·필터 적용) 위에서 정렬, 행클릭·드로어 유지. ⑦ **수량 열 제거**: 두 목록 모두 수량 컬럼 삭제→장비명에 NameWithQty(2대+만). **프런트 전용**, type-check+build 통과, 콘솔0. 적대적 리뷰 4렌즈: 확정 1건(도입 드로어 EqProjectDrawer가 isRegRequired 가드 없이 누락 황색 표기)=수정. 나머지 0.
- **장비관리 UX 개편 v2(도입배치 그룹핑·도입 3뷰·운영 장비대장, 2026-06-30)**: 시안 `docs/mockups/equipment-management-v2.html` 기준. ① **도입배치 그룹핑**(`eqSlice.buildGroups`, 로더+`recomputeEq` 공용): key=정규화장비명(baseName)+일정시그니처(start+stages)+담당자+내외자 동일시 1행. **일정 다르면 절대 합치지 않음**. 실데이터 결과 **20종·29대·24도입배치**(Spin Coater 일정 2건→2행, Hot plate 1행×4대, Wet-Station은 baseName으로 묶고 variantNames(Acid/Alkali/PR Strip 등) 세부표기). `EqGroup`에 repCode·start·stages·variantNames 추가. reducers `codes[]`화(배치 전체 적용)·`recomputeEq`서 groups 재파생(낙관적 드래그 반영). ② **장비도입**(`Equipment/index.tsx` 전면 재작성): 요약 4타일(전체 20종·29대 / 진행중·지연·미입력 종수, selectEqCounts), 워크스페이스(보기전환 **타임라인/단계별/목록** + 필터 단계·담당자·내외자·검색 + **일정편집 토글**=켤 때만 드래그). 타임라인=밀도↑ 배치행(수량배지·단계dot·담당·관리번호, 오늘선 헤더범례). 단계별=칸반(착수전+6단계+설치완료, phaseChip 분류, 미정→착수전 폴백). 목록=8열 테이블. 드래그/리사이즈→배치 전체 code `updateSchedule`(Promise.all)·저장확인 모달(대상 N대)·undo/redo(codes[])·딥링크(name|code)·`EqProjectDrawer` 배치인식(수량·관리번호범위·총금액·세부구성·등록정보 누락 황색·지연 박스). ③ **장비운영**(`EquipmentOps/index.tsx`): 카드→**장비대장 테이블**(관리번호·장비명·수량·분류·담당자·운영상태·설치장소·누락정보칩·최근이력). 요약 3카드(전체 29대·20종+분류/담당자, 운영상태 dominant, 필수정보 누락 종·대). 필터 상태·분류·담당자·검색·**누락정보만**. `EqDetailDrawer` 배치인식(부제 수량·범위·분류·내외자)+누락 상태박스(황/녹)+세부구성+미등록 황색 — 상태변경+사유·운영이력(repCode·refreshTick)·인라인편집·저장확인·권한 **전부 보존**. ④ **공용** `Equipment/batchUtil.tsx`(QtyBadge·codeRange·missingLabels). 검색은 **2번째 이후 관리번호로도** 동작(codes 전체). `GlobalSearchDialog` 장비 검색은 **동명 배치 1건으로 합산**(종 기준, key 중복·중복결과 제거 — 리뷰 확정 1건 수정). selectEqCounts·홈 EqSummaryInner는 baseName 그대로(영향 없음). ⑤ **모바일**: 탭/요약/필터/타임라인/칸반/대장 PC와 동일 구조 가로스크롤(스크롤바 숨김), 요약·필터 가로스와이프, Drawer 전체너비, 페이지 가로스크롤 없음. **프런트 전용**(백엔드/시트 무변경, 시안 하드코딩 데이터 미사용·실 API). type-check+build 통과. 적대적 리뷰 4렌즈(grouping·intro·ops·css): 확정 major 1건(GlobalSearch 동명 key/딥링크)=수정 / 미정 보드누락=선제수정됨(재검증 refuted) / 운영 동명 다행=시안 의도(refuted).
- **캘린더 손가락포인터 + 종류건수(visible range·팀원·검색, 종류제외) + 라벨간격 + 팀원칩 높이(2026-06-29 #2)**: ① **멀티데이 손가락 포인터**: 시작/단일은 기존 `.fc-event{cursor:pointer}`로 OK였지만 **중간·마지막 구간은 위 칸 `.fc-daygrid-day-events`가 덮어 cursor:auto(화살표)** 였음(hit area를 elementsFromPoint로 푼 것과 같은 원인). 해결: `onPointerMove`에서 `findEvAt(x,y)` 결과로 컨테이너(`.fc-team`) cursor를 `pointer`/`''` 동적 지정 — 가려진 구간 포함 모든 일정 위에서 손가락, 빈영역은 기본. (CSS 베이스 + JS 동적, 무분별 !important 없음, 호버추적·클릭핀 미변경). 실측: start/mid/last=pointer, empty=기본. ② **종류 칩 건수 = 현재 visible range ∩ 팀원 ∩ 검색** (종류필터 **제외**). 기존 `catCounts`는 전체 로딩(78건) 집계였음 → `visRange`({start,end}, FC `datesSet`의 activeStart/activeEnd, 초기값 `gridRange()`) 기준으로 재작성. `allEvents`는 날짜별 펼침이라 `ev.date`로 범위판정 + `getDay` 0/6 주말제외(주말보기 off 시) + 팀원·검색 적용 + **id 기준 dedup(멀티데이 1건)**. 종류필터는 집계에서 빼서 다른 칩이 0이 되지 않음(선택가능 건수 유지) — 달력 표시(`fcEvents`)에만 적용. 재계산 deps `[allEvents,visRange,showWeekends,selMembers,searchTrim]`로 월/주 이동·오늘·뷰전환·주말토글·팀원·검색·데이터변경 즉시 반영, 종류선택은 숫자 불변. 실측: 6월21·7월9·주3, 센터→[4,1,0,0,0,0,2]/fc7, 검색'출장'→[0,0,0,9,0,0,0]/fc9, 회의선택→숫자 유지[4,2,2,9,0,2,2]·fc4. ③ **라벨↔첫칩 간격 동일**: 기존 `@container(<=1000px)`에서 종류만 padding-left:38(절대라벨)·팀원은 inline 8px라 어긋남 → `.cal-fb`를 **2줄 컬럼 고정**, 팀원/종류 둘 다 `position:relative;padding-left:27px` + 라벨 `position:absolute;left:0`(`.cal-fb__teamlbl`/`.cal-fb__catlbl`)로 **라벨 x·첫칩 x·간격 일치**(@container 블록 삭제). 실측 라벨 x=247·첫칩 x=274·gap 7px(양행). ④ **팀원 칩 높이 28→26px**(폰트·좌우패딩·곡률·상태 유지, 종류칩과 높이 격차 축소). **프런트 전용**, type-check+build 통과, 콘솔에러0(이번 세션 새 dev server라 스테일 버퍼도 0). 적대적 리뷰 3렌즈(counts·cursor/preserve·css)=**확정 0건**. 잔여: 주말전용 일정이 현재 데이터엔 없어 주말토글 시 건수 변화는 로직만 검증(실데이터로 추후 확인 가능).
- **업무일정 분류 보완 + 멀티데이 hit area 재설계 + 캘린더 상단 조작부 재배치(2026-06-29)**: ① **[사가]→연차(leave)**: `calSlice.classify` 태그 정규식에 `사가` 추가(`/연차|반차|휴가|사가/`). **태그우선 단일 파서**(제목 앞 `[..]`만 판정, 본문 단어로는 분류 안 함; evCat은 태그 없을 때 폴백). leave 색·아이콘(#D87CA6·BeachAccess)·필터·센터fallback 공유. ② **채용(recruit) 필터 제거→기타 통합**: `CAT_ORDER`에서 recruit 제거(7탭), `classify`/`evCat`의 채용 분기 삭제→`[채용]`은 'etc'(기타 색/아이콘). 원본 제목 불변. `CAT_META`·`CAT_ICON`엔 `recruit` 키 유지(`Record<RealCat>` 타입 exhaustiveness). ③④⑤ **멀티데이 전 segment(시작·중간·마지막·텍스트 없는 이어짐) hit area** — 원인: 멀티데이 막대(`.fc-event`)는 시각적으로 여러 칸을 덮지만 다른 칸의 `.fc-daygrid-day-events` 컨테이너에 **가려져** `elementFromPoint`(단수)는 덮개만 잡음. 해결: **컨테이너 위임 + `document.elementsFromPoint`(복수)** 로 z-스택을 훑어 `.closest('.fc-event')` 중 `detailMap`에 등록된 막대를 찾음(`findEvAt`). `eventDidMount`는 isStart/isEnd 무관 **모든 segment el→detail**을 WeakMap에 등록(`eventWillUnmount`에서 제거+locked면 닫기). `.fc-team` 컨테이너에 `onPointerMove`(호버 포인터추적 유지·일정 밖 나가면 닫힘)·`onPointerLeave`·`onClick`(단일 클릭 경로, `stopPropagation`로 바깥닫기 차단, 같은 segment 재클릭=닫기/다른 segment=전환). 월간·주간, 시작·중간·마지막·빈영역 실측 OK(`elementsFromPoint`로 mid/last에서 fc-event 검출 확인). ⑥~⑩ **상단 툴바 1행화(space-between)**: 왼쪽=[월/주 토글]+[‹|오늘|›] **단일 외곽선 버튼그룹**(얇은 세로 구분선·`overflow:hidden`으로 바깥 모서리만 둥글게·Chevron 아이콘·hover/active/focus-visible·평소 채움 없음)+년월. 오른쪽=검색input(flex 240·maxWidth 260)·주말보기(gap 8px). **검색을 필터박스에서 분리**(CalFilterBar에서 search/onSearch·`.cal-fb__search` JSX 제거, 고아 CSS도 정리). 반응형(xs): 1행 nav+년월+주말, 2행 검색 풀폭(order/flex-basis), 가로 스크롤 없음. ⑪~⑯ **보존 검증**: 팀원필터 풀네임/패딩/곡률, 초승달 겹침, 첫 해당자 이름, 호버 포인터추적, 클릭위치 상세, 원본제목 상세, 센터 fallback, 메모답글, 게시판답글, 소프트삭제, +N — 모두 무회귀. **프런트 전용**(백엔드/clasp 무변경), type-check+build 통과, 콘솔 신규에러0(잔존 96건은 HMR 편집중 스테일 — 풀리로드+재진입해도 카운트 불변·전부 옛 `?t=` 모듈, 7탭/21이벤트/3네비 정상 렌더로 확인). 적대적 리뷰 3렌즈(hit-area·toolbar·classify/preserve)=확정 0건(유일 blocker '클릭핀이 document 닫기리스너로 즉시닫힘'은 React18이 `#root`에 위임+synthetic stopPropagation→native stopPropagation이라 document까지 안 감 → **고신뢰 오탐**, 라이브 클릭핀 동작과도 일치). **잔여(사용자)**: `[사가]` 실데이터 1건으로 라이브 최종 확인 권장.
- **일정 칩/필터 보완 + 연차 카테고리 + 개선메모 답글(2026-06-28)**: ① 팀원필터 칩 좌우패딩 11→8px·곡률 8→10px(풀네임·높이·색 유지). ② 일정 해당자 첫칩 좌우패딩 7→5px·곡률 6→8px(`CHIP_RADIUS`, 뒤쪽 초승달 칩 동일 곡률·겹침 유지). ③ event bar 우측 내부패딩 7→4px(`.fc-team .fc-daygrid-event`)로 해당자 칩이 bar 우측에 더 붙음(검증 barRightGap 4px). ④ 멀티데이 정렬은 단일 일정 실측 인셋 재사용이라 패딩 변경에 자동 적응(짧은 멀티데이 vs 단일 Δ0px 재검증). ⑤ 멀티데이 전체 segment hit area — 기존 eventDidMount(per-segment)가 이미 처리. **주경계 분할 이벤트 주입해 continuation segment 빈영역 호버·클릭 고정 실측 OK**(이전 라운드 의심 해소). ⑥ **연차 카테고리(leave)**: `CalCatId`+`catMeta`(label 연차·color #D87CA6 로즈, 테마 accent 없어 catMeta 리터럴)+`CAT_ORDER`, `calSlice.classify` [연차]/[반차]/[휴가] 태그우선 매핑, `CAT_ICON.leave=BeachAccessIcon`(ChipContent·CalFilterBar). 필터탭·아이콘·색·연차일정·필터링 라이브 검증(연차 3건 실데이터). ⑦ **개선메모 답글**: `PageImprovementMemo`에 파란 `답글 +N` 칩 + 펼침영역에 `ReplyThread` 통합(번호·제목·작성자·대상 → 내용 → 답글 +N → 목록 → 입력창). 기존 replySlice·ReplyThread·reply API·시트 재사용(데이터 복제 없음, 게시판과 양방향 동기화). 단일오픈, 본인 답글만 수정/삭제(인라인·확인Dialog), 관리자만 작성. 합성 메모+답글 주입해 칩·목록·작성자별 권한·composer 렌더 검증. **프런트 전용**(백엔드 무변경), type-check+build 통과, 콘솔 신규에러 0. 적대적 리뷰 9에이전트(6건 중 1확정): `CAL_CATS`(홈 대시보드용)에 leave 누락 → 홈에서 연차가 파랑'기타'로 표시되던 것 수정(`#D87CA6` 연차 추가). 나머지 5건 오탐.
- **라이브 수정 5건(2026-06-28)**: ① 좌상단 로고/브랜드 **원복**(TopBar 원본 `<img>`·index.css 크롭/텍스트 제거) — 브라우저 탭 제목만 `ANGELS PORTAL` 유지. ② 해당자 칩 알약(트랙)→**둥근사각형 radius 6px**(`ChipContent` CHIP_RADIUS), 팀원탭 radius 8px(`CalFilterBar`). 높이·글자·겹침·z-index 유지. ③ 멀티데이 해당자 위치 **정확 정렬**: 추정값(-14) 제거, 단일(인플로우) 일정의 '셀우측-event-main우측' 인셋을 측정해 `group.right = 막대시작셀.right - 단일우측인셋`(abs harness 좌측오프셋 무관). 검증: 짧은 멀티데이(여수) vs 단일 Δ0px, 긴 제목(부산)은 제목 말줄임 없이 제목 바로 뒤 배치. ④ 멀티데이 **빈 segment hit area**: `eventMouseEnter/Click` 프롭 제거→`eventDidMount`로 각 `.fc-event` 전체에 mouseenter/leave/click 직접 바인딩(+`eventWillUnmount` 정리, WeakMap). 빈 영역 호버/클릭 OK 검증. **호버 포인터추적(onMouseMove)·클릭 고정은 그대로 유지(미수정)**. ⑤ 답글 `알 수 없는 action` = 라이브 Apps Script 구버전 탓(코드 action 대소문자 일치 확인됨, 버그 아님) → **clasp 재배포 완료**(사용자 승인 후 로그인→push→기존 배포 @56→@57 재배포, 동일 /exec URL). 라이브 `?action=getReplies`=`{"status":"ok","items":[]}` 확인, getImprovements 정상. type-check+build 통과, 콘솔 신규에러 0. **남은 검증(사용자, 로그인 필요)**: 라이브 포털에서 답글 등록·+N 증가·수정·수정됨·삭제·새로고침 유지 E2E.
- **A.브랜드 ANGELS PORTAL 개명 + B.포털개선요청 답글기능 + C.업무일정 UX 보완 구현·검증(2026-06-28)**:
  - **A**: 탭 제목·TopBar·package.json 브랜드를 `ANGELS PORTAL`로. TopBar는 합본 로고(ANGELS마크|FAB구축포털시스템)를 좌측 마크만 CSS 크롭(`.topbar-logo-wrap` 36px) + `ANGELS PORTAL` 텍스트. FAB 구축 로드맵/구축팀 등 **업무 명칭은 미변경**.
  - **B (답글)**: 신규 시트 `포털개선답글`(헤더: 답글ID·요청번호·작성일시·작성자·답글내용·수정일시·삭제여부). `Code.gs` 추가: `replyCtx_`(헤더탐지·시트없으면 생성)·`getReplies_`(doGet `action=getReplies`, 삭제 제외)·`createReply_/updateReply_/deleteReply_`(doPost, LockService, 관리자 인증, 본인작성만 수정/삭제, **소프트삭제** 삭제여부=TRUE). `api/sheets.ts` `fetchReplies/createReply/updateReply/deleteReply`. 신규 `replySlice`(loadReplies + 낙관적 add/patch/remove) store 등록, MainLayout 진입로드. Improve 페이지: 제목옆 파란 `답글 +N` 칩(점 없음·삭제 제외 집계), **단일오픈 아코디언**(openId), 펼침영역에 원문+`ReplyThread`(시간순 목록·작성자·YYYY-MM-DD HH:MM·수정됨·본인 인라인수정·삭제확인 Dialog·관리자 입력창). N+1 없이 1회 로드 그룹화. **백엔드 git 커밋만, clasp 배포는 사용자 승인 후**(미배포 시 프런트는 빈 답글·등록 시 에러로 graceful degrade).
  - **C**: 해당자칩 원형→**알약(이름)+뒤쪽 초승달 조각**(`ChipContent` PillStack, 첫칩 z-top, 깊게겹침). 단일=칸 우측끝·멀티데이=첫칸 우측끝(groupRef 측정). 호버 상세 **포인터 추적**(`.fc-team` onMouseMove, 호버 pointerEvents:none로 깜빡임 방지)·클릭 고정. 빈영역/멀티데이 구간 hit OK(FC eventMouseEnter/Click). 주간 2줄 해당자 **우측정렬**. 팀원탭 원형→**알약(이름 크게)**(`CalFilterBar` MemberPill).
  - 검증: type-check+build 통과. 라이브 dev: A 로고/탭=ANGELS PORTAL(FAB 누출 없음), C 알약칩·크레센트·멀티데이 첫칸·호버추적(left634→414)·클릭고정·주간2줄 우측·콘솔0, B 아코디언/입력창/빈상태 렌더·degrade OK. 적대적 리뷰 12에이전트(9건중 1확정=답글 낙관적추가 created 폴백, 수정). **잔여(사용자)**: ① Apps Script `clasp` 배포 승인(답글 동작) ② (선택)`포털개선답글` 시트 자동생성됨.
- **캘린더 해당자·상세·포털 새글 규칙 전면 개편 구현·검증 완료(2026-06-28)**: ① 월간 해당자 = 첫 칩 이름 + 나머지 초승달 포개기(첫번째 z-top), 단일=칸 우측끝·멀티데이=첫칸 우측끝(제목 길면 제목 뒤로, 측정기반 `ChipContent` reserve). ② 주간(timeGridWeek) 여러 시간칸 일정=2줄(아이콘·시간·제목 / 전체 해당자 이름, +N 없음). ③ 상세=원본 제목 그대로(`rawTitleNoTags`, 장소-목적 파싱 폐지) + 시간 + 전체 해당자. ④ 호버/클릭 상세를 MUI Tooltip→**마우스 위치 커스텀 팝오버**(`EventPopover`, 뷰포트 보정·빈영역 클릭·멀티데이 각 구간, z-index 10000). ⑤ 해당자 없으면 센터(`eventMembers`). ⑥ 새글 공용 유틸 `utils/newPost`(isNoticeNew/isWorkNew/isImproveNew) — 페이지 N과 사이드바 **동일 함수**. `isRecentNew` 창=등록일 포함 7일(diff 0~6). 공지=게시일7일+종료제외 / 업무=진행중+발의7일(완료·Remind 제외, Check 무관) + 진행중 카드에 N배지(`TaskAccordion`) / 개선=접수·검토중·보류+7일(완료·불가 제외). 업무일정 사이드/하단 배지 제거. ⑦ 사이드 배지 재설계: 새글=행 우측끝 빨강 숫자(볼드X·0숨김·99+) / 개선메모=메뉴명 우상단 노란 점(숫자 없음, 관리자 전용). **프런트 전용**, type-check+build 통과, 라이브 dev 적대적 리뷰(9건 중 1확정=팝오버 z-index, 수정) + 브라우저 검증(콘솔0). 잔여 정리: WeekBoard.tsx·ChipTooltip.tsx 데드코드(별도 정리).
- **사이드 배지 + 캘린더 한 줄 칩 개편 구현·검증 완료(2026-06-28)**: ① PC 사이드 메뉴 위첨자 배지를 아이폰 알림 스타일로 — 새글=빨강·개선메모=노랑 원형, 색+숫자만(N·아이콘 제거), 메뉴명 뒤 나란히, 15px, 0숨김/99+. 기존 행우측 회색 pill 제거(`SideNav.tsx`+`index.css` `.snav-labelwrap`/`.sup-badge`). ② 업무일정 월간 캘린더 칩을 **한 줄**로(`ChipContent.tsx` 재작성, `dense` 제거): 구분아이콘→시간→장소목적→해당자칩, 종일·멀티데이 포함. 해당자는 제목 바로 옆(5px), 우측 끝 안 밀림(title flex:0 1 auto). ③ 호버 상세 신규 `ChipTooltip.tsx`+`members.splitPlacePurpose()`: 칩은 "장소-목적" 그대로, 호버서만 목적=제목·장소=세부정보 분리(월간·주간 공통). **프런트 전용**, type-check 통과, 라이브 dev 검증(콘솔0). 상세: bridge `inbox/claude-to-codex.md`.
- **STEP27(새 업무 인라인폼 개편 + 완료 다이얼로그 Remind 체크박스) 구현·검증 완료**: 진행중 인라인 새업무 카드 입력 보조 개편 + 완료 확인 다이얼로그 Remind 토글. 신규 공용 위젯 `Work/inlineFields.tsx`(ComboField 드롭다운+자동완성 / DateField 한글라벨'발의일자·예정일'+showPicker / TimeRangeField 시작·종료 wheel picker→"HH:MM ~ HH:MM" / LinkButton 제목우측 외부링크 팝업 / AttachButton 준비중 자리만). 구분·담당자·부서·장소 옵션은 전체 업무 히스토리(items)에서 수집(index.tsx `fieldOptions`). 완료시 `updateWork({remind: 체크값})`. **프런트 전용(시트/백엔드 무변경)**, type-check+build 통과, 라이브 dev 검증(콘솔0). 상세: `docs/step27-newtask-form-revamp.md`. ⚠ 구분·담당자는 freeSolo(드롭다운+직접입력) — 엄격 드롭다운 원하면 추후 조정.
- **STEP26(업무 KPI 전체타일·목록 2열·들여쓰기) 커밋·자동배포 완료**: KPI에 '전체' 타일(완료/Check 사이) + 상태칩행 제거 · 진행중/완료/Check 목록 2열 아코디언(진행중 펼침/완료·Check 접힘, `CardGrid columns={2}` 반응형) · Remind 카드 담당자 우측(날짜와 그룹) · 글머리기호 행잉 인덴트(공용 `Work/SubLine.tsx`). 프런트 전용.
- **STEP25(업무현황 KPI/Remind 정리) 커밋·자동배포 완료**: KPI 보류·취소 타일 제거→진행중/완료/Check/Remind 4타일 · '검토'→'Check'(chief 칩 전부) · Remind는 KPI 타일 토글로 KPI 아래 펼침(하단 상시섹션 제거) · Remind 카드 압정아이콘+상태/구분/담당자/날짜(YYYY-MM-DD)·'발의' 삭제. 프런트 전용. (이하 STEP23·24·어휘개편도 모두 배포 완료 — 아래 '미커밋' 표기는 과거 스냅샷.)
- **STEP24(업무현황 회의 뷰)**: /work 개편 — 업무목록을 KPI 바로 아래로, 기본 진행중, 진행중=아코디언(모두 펼침·개별 접기, 신규 `Work/TaskAccordion.tsx`), RatioBar 제거, '긴급 업무'→'Remind', 담당자현황 숨김(`SHOW_MANAGER_STATUS=false`, 코드 보존), 검토필요→'검토'. 프런트 전용. type-check+build 통과, 라이브 검증 + 적대적 리뷰(9건 중 3확정: TaskCard 라벨·아코디언 장소중복 수정, urgent 변수명은 지시대로 미수정).
- **STEP23(상태 변경 사유 입력, 미커밋)**: 즉시저장 → 확인 Dialog(장비명·관리번호·전→후·사유 optional·trim) → `updateEquipment({state, reason})`, 운영이력에 사유 기록, 같은상태 no-op, 이력 표시 '작성자 · 사유'. 프런트 전용(**백엔드 변경/배포 없음** — reason은 STEP22부터 기록). type-check+build 통과, 라이브 dev에서 Dialog 흐름·no-op·취소 확인(콘솔0).
- **장비 상태 어휘 개편(사용자 직접 요청, 미커밋)**: 표시 라벨 매핑 폐지 → 시트값 그대로 표시. 정식 4값 `도입예정/도입중/운영중/비가동`(가동중→운영중, 설치중→도입중으로 통일). 4값 외(오타·빈값·레거시 '가동중')는 상태칩에 **'미분류'**(neutral). type-check+build 통과, 라이브 dev 검증(KPI 라벨 verbatim, E-Beam Lithography='미분류', 콘솔0). **⚠ 마이그레이션 필요**: 시트의 기존 '가동중' 등을 새 4값으로 고쳐야 함(안 고치면 미분류로 표시). 변경 파일: types/index.ts·eqMeta.ts·selectors.ts·EquipmentOps/index.tsx·EqDetailDrawer.tsx·Home/dash/(KpiOverview·EquipmentSection)·previews.tsx·EqSummaryInner.tsx·EqItem.tsx.
- STEP22 phase 1(장비 운영이력) implemented + reviewed + fixed + **DEPLOYED & LIVE & E2E-VERIFIED** (frontend `87a71c0`; backend clasp **@42**). Live E2E: a user-performed admin status change on **CL-001 (Spin Coater)** auto-created `장비운영이력` with 2 rows (도입예정→도입중→가동중); `?action=getEqHistory` returns them. Review-fix pass applied (menu compares raw state; explicit history error/loading).
- Backend `Code.gs`: append-only `장비운영이력` sheet (`appendEqHistory_`/`getEqHistory_`), and `updateEquipment_` appends one row only when state actually changes. Frontend: `fetchEqHistory` + read-only 운영 이력 drawer section.
- 3-lens adversarial review found 8, confirmed 3 (1 med stale-history race, 2 low: label fallback, dup repCode) — all fixed in `EqDetailDrawer.tsx` (unified guarded load via refreshTick; raw label for non-standard states; single repCode). 5 refuted.
- STEP21 status dropdown + anchorEl guard are now committed (`87a71c0`) and live.

## Last Known Verification

- 2026-06-30 #2: 주간 겹침/타임라인/목록/누락. `npm run type-check`+`npm run build` 통과, 콘솔0. 라이브 dev(admin): 주간뷰 동시 3건→3등분(overlaps0)·호버/cursor pointer. 타임라인 24행 minHeight32·Hot plate'4대'텍스트·Laser Machine 수량없음·pageOverflow false(PC/모바일 둘다·gantt fit)·일정편집 후 Hot plate 드래그 +1개월(2028.7→2028.8, measureHalfPx 42px/월) 정확·취소. 도입목록 헤더=관리번호/장비명/담당자/구분/현재단계/다음일정/총도입금액(수량열 없음)·장비명 asc/desc 아이콘. 운영대장 헤더 수량열 없음·누락열 '—'(도입예정)·필수정보누락 요약 0종·0대·담당자 정렬. 도입 드로어 등록정보 '누락' 미표기(도입예정). 적대적 리뷰 4렌즈(1확정=도입드로어 가드 수정). **커밋·자동배포 진행.**
- 2026-06-30: 장비관리 v2. `npm run type-check`+`npm run build` 통과. 라이브 dev(admin) 검증: 그룹핑 24배치(Spin Coater 2행/Hot plate 1행4대/Wet-Station 변형 4행). 장비도입 — 요약 20종·29대, 3뷰(타임라인 24행/목록 24행/단계별 칸반) 전환, Hot plate 드로어 '4대·HT-001 외 3·총 15,200천원·누락4', **일정편집 후 드래그→저장확인 "4대 배치 전체 2028.7→2028.8" 후 취소**(라이브 미저장). 장비운영 — 장비대장 24행·요약(29대·20종/도입예정28/누락 20종·29대)·운영상세 'Hot plate 4대'+누락 황색박스+상태변경/이력 보존, **HT-002(2번째 관리번호) 검색→Hot plate**·누락정보만 토글. 모바일(390) — 요약/필터 가로스와이프·타임라인 좌측명+가로스크롤·장비대장 가로스크롤·페이지 가로오버플로 없음·Drawer 전체너비. GlobalSearch 동명 합산(Spin Coater 1건 2대/Wet-Station 1건 5대). 콘솔 에러 0. **커밋·자동배포 진행.**
- 2026-06-29 #2: 캘린더 손가락포인터/종류건수/라벨간격/팀원칩높이. `npm run type-check`+`npm run build` 통과. 라이브 dev(1280px, admin): 커서 — 멀티데이 start/mid/last 모두 pointer(가려진 구간 onPointerMove 동적), 빈영역 기본. 건수 — 6월21·다음달7월9·오늘복귀21·주간뷰3, 팀원 센터선택 [4,1,0,0,0,0,2]/fc7·해제복귀21, 검색'출장' [0,0,0,9,0,0,0]/fc9·삭제복귀21, **회의 종류선택 시 숫자 유지[4,2,2,9,0,2,2]·달력만 fc4**(종류필터 집계제외 확인), 주말토글 무변(주말전용 일정 없음). 라벨/칩 — 팀원·종류 라벨 x=247 동일·첫칩 x=274 동일·gap 7px 동일, 팀원칩 높이 26px. FC 그리드 풀폭(1020px·평일5칸). 콘솔 에러0. 적대적 리뷰 3렌즈 확정0. **커밋·자동배포 진행.**
- 2026-06-29: 업무일정 분류/멀티데이 hit area/상단 조작부. `npm run type-check`+`npm run build` 통과. 라이브 dev(admin): classify 단위검증(`[사가]`/`[연차]`/`[반차]`/`[휴가]`→leave, `[채용]`→etc, 태그우선=`[회의] 사가관련`→meeting, 본문단어 무태그→evCat). 멀티데이 `document.elementsFromPoint`로 시작(idx0)·중간(idx2)·마지막(idx2)에서 `.fc-event` 검출 → 호버·클릭 동일 상세 오픈(start0.1/mid0.5/last0.9). 툴바 1행: 월/주+[‹|오늘|›]그룹+년월(왼쪽)·검색+주말(오른쪽), 채용탭 제거(7탭)·연차탭 존재, 다음→7월·오늘→6월 동작, 모바일 폭 검색 2행 래핑. 풀리로드+홈→캘린더 재진입 = filterTabs7/events21/navBtns3/teamPills5 정상(콘솔 에러 카운트 96 불변=스테일 HMR). 적대적 리뷰 3렌즈(4에이전트)=확정 0. **커밋·자동배포 진행.**
- 2026-06-28: ANGELS PORTAL 개명 + 답글기능 + 일정 UX. `npm run type-check`+`npm run build` 통과. 라이브 dev(admin): A=탭/브랜드 'ANGELS PORTAL'·로고 크롭(FAB 누출 없음). C=알약 해당자칩·2인 크레센트(현진+조각)·멀티데이 첫칸 우측끝(여수/부산)·호버 상세 포인터추적+pointerEvents none·클릭 고정(이동 안함)·빈영역 hit(여수 우측 빈칸 호버 OK)·주간 tall 2줄 우측정렬·팀원 알약탭·콘솔0. B=Improve 아코디언 단일오픈·입력창/빈상태 렌더(백엔드 미배포라 답글 0·등록 시 graceful 에러). 적대적 리뷰 12에이전트(9→1확정: 답글 created 폴백 수정). **답글 동작은 clasp 배포 후**(사용자 승인 대기). 프런트는 커밋·자동배포 진행.
- 2026-06-28: 캘린더 해당자·상세·새글 규칙 개편. `npm run type-check`+`npm run build` 통과. 라이브 dev(admin) 검증: 월간 단일=칸 우측끝(trailingGap 0)·멀티데이 첫칸 우측끝(여수 2칸 left187/cell204·부산 3칸·긴제목 강제시 left336>204 말줄임X)·첫칩 이름+초승달·센터 fallback / 주간 tall 일정 2줄(height359 column, 세리·성범) / 호버=마우스 근처 표시·leave 닫힘·클릭=빈영역서도 고정·뷰포트 내·원본제목 "동탄(ZEISS)-FIB 데모테스트"·"국내(한양대)-..." 그대로 / 사이드 공지[빨강1]·업무일정[노란점·숫자없음]·개선[빨강1] / 공지 종료건 N제외·개선 페이지N=사이드(검토중1)·업무 새업무0(진행중6 모두 발의7일초과, page=side 일치). 콘솔 에러0. 적대적 리뷰 12에이전트(9findings→1확정: 팝오버 z-index 1350<FC더보기팝오버 9999 → 10000으로 수정). **커밋·자동배포 진행.**
- 2026-06-28: 사이드 배지 + 캘린더 한 줄 칩 개편. `npm run type-check` 통과. 라이브 dev(1280px, admin) 검증: 사이드 공지[2빨강]·업무일정[6빨강][1노랑]·업무현황[6빨강](계산값 #E05B54/#D6A23E·h15px); 캘린더 전 칩 한 줄(20~25px)·멀티데이 3건 해당자 옆(측정 titleToLast=5px)·호버 Tooltip 목적/장소 분리. 콘솔 에러 0. 프런트 전용. **커밋·자동배포 진행.**
- 2026-06-15: Claude reported `npm run type-check` and `npm run build` passed for STEP21.
- 2026-06-16: Codex ran `npm.cmd run type-check`; passed.
- 2026-06-16: Claude ran browser/admin verification in dev; dropdown and guest gating OK.
- 2026-06-16: Claude added `anchorEl` guard to status `Menu`; type-check passed.
- 2026-06-16: Codex re-ran `npm.cmd run type-check`; passed.
- 2026-06-16: Claude implemented STEP22 phase 1 + review fixes; `npm.cmd run type-check` + `npm.cmd run build` passed; fresh dev server runtime had 0 console errors, drawer shows 6 sections incl. read-only 운영 이력 (empty handled gracefully while backend undeployed), admin gating intact.
- 2026-06-16: **DEPLOYED** — frontend GitHub Actions "Build and Deploy" success for `87a71c0`; backend `clasp redeploy @42`; live GET `?action=getEqHistory` returned `{status:ok, items:[]}`. Remaining: live admin status-change → history-record end-to-end check (needs a designated safe test equipment).
- 2026-06-16: Codex reviewed STEP22 phase 1 again and ran `npm.cmd run type-check` (passed). Follow-ups: raw non-standard state menu comparison, explicit history fetch error/loading state, and bridge/docs live-E2E verification inconsistency.
- 2026-06-16: **Live E2E CONFIRMED** — user performed an admin status change on CL-001 (Spin Coater) on the live site; `장비운영이력` auto-created with 2 rows (도입예정→도입중→가동중, 작성자 조성범, 01:35 KST); `?action=getEqHistory` returns them. (Side effect: CL-001 live state is now '가동중' — revert via UI if unintended.) Review-fix pass (raw-state menu compare + histError/loading) applied; `npm.cmd run type-check` passed.
- 2026-06-16: 장비 상태 어휘 개편(라벨매핑 폐지·운영중/도입중 verbatim·미분류 폴백) 구현. `npm.cmd run type-check` + `npm.cmd run build` 통과; 라이브 dev 검증(운영중/도입중 KPI 라벨, E-Beam Lithography 카드='미분류' 칩, 콘솔0). **미커밋** — 배포 전 시트 기존 '가동중' 값 마이그레이션 권장.
- 2026-06-16: STEP23(상태변경 사유 확인 Dialog) 구현. `npm.cmd run type-check` + `npm.cmd run build` 통과; 라이브 dev(@42): 상태 선택→확인 Dialog(장비명·관리번호·전→후·사유), 같은상태 no-op, 취소 정상, 콘솔0. 실 저장 성공경로는 라이브 데이터 변경이라 미수행(타입체크+STEP22 경로 재사용으로 갈음). **미커밋**.
- 2026-06-16: STEP24(업무현황 회의뷰) 구현 + 적대적 리뷰 수정. `npm.cmd run type-check` + `npm.cmd run build` 통과; 라이브 dev(/work): 업무목록 KPI 바로 아래·기본 진행중·아코디언7 모두 펼침·개별 접기(7→6)·전체탭→컴팩트행·Remind 워딩·담당자현황 숨김·'검토' 단축·장소 중복 해소(maxLoc=1)·콘솔0. **미커밋**.
- 2026-06-16: STEP23·24·상태 어휘 개편 **커밋·자동배포 완료**(f8bfee3). 이후 STEP25(KPI 보류/취소 제거·Check·Remind 토글·카드 재구성) 구현 + type-check/build 통과 + 라이브 dev 검증(서버 재기동 후 콘솔0, KPI 4타일·Remind 토글·압정카드·YYYY-MM-DD) → **커밋·자동배포 완료**. 운영 규칙=자동배포.
- 2026-06-16: STEP26(KPI '전체' 타일·상태칩행 제거·목록 2열 아코디언·SubLine 들여쓰기 공용화) 구현 + type-check/build 통과 + 라이브 dev(서버 재기동, 1280px) 검증(KPI 5타일 전체가 완료/Check 사이·진행중 2열 펼침/완료 2열 접힘·Remind 담당자 우측·글머리기호 분리·콘솔0) → **커밋·자동배포 완료**.

## Decisions

- Use `.agents/bridge` as the shared handoff area.
- Use role-specific inbox files to avoid overwriting each other's notes.
- Use `lock.md` before source edits when both agents may be active.
- For this repository, run type check with `npm.cmd run type-check` on Windows.
- Claude Code is responsible for development implementation and git push/pull.
- **Codex = 비개발자 사용자를 위한 UX/UI/기능 자문위원**(루트 `AGENTS.md` 기준): Claude 보고 요약·업무적 의미 설명·시각 예시/선택지·추천 제공. **사용자가 명시 요청하지 않는 한 Claude용 개발 프롬프트는 작성하지 않음**(이전 '다음 프롬프트 작성' 규칙은 폐지).
- Codex should avoid source edits, git sync, and screenshot capture unless the user explicitly overrides the role split.
- Immediate status apply is acceptable for now; do not add a confirm modal unless user feedback says it feels risky.
- Unknown non-empty equipment states may continue falling back to `비가동`, matching the existing count logic.
- STEP22 phase 1 uses a separate append-only sheet named `장비운영이력`.
- STEP22 phase 1 history columns: `일시`, `관리번호`, `장비명`, `이전상태`, `변경상태`, `사유`, `작성자`, `작업유형`, `비고`.

## Warnings

- Do not run destructive git commands unless the user explicitly asks.
- Do not let both agents edit the same file at the same time.
- Do not store credentials in bridge files.
- Do not have Codex and Claude Code implement changes concurrently.
- Do not mutate live equipment status unless there is a safe test record or explicit user approval.
- Do not deploy Apps Script until the user explicitly approves backend deployment.

## Next Handoff

- Codex(자문위원): STEP23·24·25·26 + 상태 어휘 개편 **모두 커밋·자동배포 완료**. 사용자에게 현황을 쉽게 요약하고, 다음 후보(STEP22 phase2 / 담당자현황 재노출 / 보류·취소 필터 재노출 / 완료·Check '모두 펼치기' / 발표모드 등)를 **선택지·장단점·추천**으로 제시. (Claude용 프롬프트는 사용자가 명시 요청할 때만 작성.) **운영 규칙**: 작업 완료 시 자동 커밋·푸시(자동배포). **잔여(사용자)**: 시트 '가동중'→'운영중' 마이그레이션. 백엔드 무변경(@42).
