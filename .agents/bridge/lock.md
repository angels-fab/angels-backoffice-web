# Work Lock

status: free
current_worker: none
task: (피드백 4건 완료 — 연필수정 복귀(안숨음)·수정칸 틴트+밑줄·1열 우측정렬(3)·자유폭 %저장+1·2열 자석스냅(엣지 반대쪽 고정). DB 마이그레이션 2건(RPC·체크제약 확장). 전부 라이브 실측. 커밋·자동배포)
started_at: 2026-07-12

## Notes

- Set `status: locked` before editing shared source files.
- Clear back to `status: free` when the work is finished, committed, or handed off.
