# STEP19 — 장비운영관리 상세 Drawer 고도화 (조회 전용)

> 작성: 2026-06-15 (사무실 PC) · 상태: 완료, main 배포 (게스트 E2E 검증 완료)

## 목표
`EqDetailDrawer`를 운영에 필요한 전 정보를 **섹션**으로 보여주게 고도화. **조회 전용**(편집/CRUD/저장 없음 — STEP20 예정).

## 변경 파일
- `types/index.ts` · `EqGroup`: 대표값 4필드 추가 — **assetNo, nfec, installDate, mgr2(엔지니어)**. (EqRawItem엔 있었으나 그룹에 미전파였음)
- `store/slices/eqSlice.ts`: 그룹 생성 시 위 4필드를 **대표(첫 개체)** 값으로 전파.
- `pages/EquipmentOps/EqDetailDrawer.tsx`: 상단(상태·분류 칩 + 관리번호) + **5섹션** + 빈값 처리 + 폭 확대.

## 표시 항목 (섹션)
- 상단: 장비명 · 관리번호 · 상태 · 분류
- 기본 정보: 담당자 · 장비종류 · 제조사 · 모델명 · 자산번호 · NFEC번호
- 설치 정보: 설치장소 · 설치일자
- 업체 정보: 업체명 · 엔지니어 · 연락처
- 예산 정보: 도입금액 · 재원
- 기타: 비고

## UI/UX
- 다크 테마 유지. 빈값은 **"미등록"**(빈칸 출력 금지). 폭 **480→520**. 긴 장비명은 AppDrawer 제목(h3) 자동 줄바꿈. 다중 대수면 "대표 1대 기준" 주석.

## 검증 (게스트 E2E — 읽기 전용이라 직접 확인 가능)
- Drawer 오픈·데이터 매핑 정상(예: Spin Coater "공정·2대", 담당자 박주봉, 도입금액 24,200 천원, 재원 지방비), 미등록 처리 정상, 5섹션 렌더, 콘솔 에러 0.
- `npm run type-check`·`npm run build` 통과. 검색/필터 무영향. **CRUD/저장 코드 없음.**
