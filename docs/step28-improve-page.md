# STEP28 — 개선제안 페이지 신설 (소통 그룹)

> 2026-06-19 · 프런트 + 백엔드. 백엔드 clasp 배포 **@43**(URL 불변), 프런트 `main` 자동배포. type-check·build 통과, 라이브 dev 검증(콘솔0, 실데이터 4건).

사용자가 홈페이지 개선 의견을 남기고 관리자(담당자)가 반영 상태를 관리하는 게시판.

## 데이터 (구글시트 '개선사항' 시트)
- 헤더 **3행**(자동 탐지), 실제 헤더: `번호·유형·개선위치·긴급·제목·관련자료·작성자·제안일자·개선내용·담당자·상태·완료일자·사유`
- 백엔드는 헤더명(+동의어)으로 인식 → 열 순서 비의존. `사유`는 불가·보류 **공용 열**.

## 백엔드 (google-apps-script/Code.gs, @43)
- `improveCtx_()` — '개선사항' 시트 헤더 자동 탐지(첫 10행에서 '제목' 기준) + 동의어 col 맵.
- `getImprovements_()` (GET `?action=getImprovements`) — 헤더명 기준 객체 배열(+headers) 반환. 조회 인증 불필요.
- `createImprovement_()` (POST) — 로그인 사용자(담당자 시트 인증)만. 상태=접수중, 제안일자=오늘(미지정 시), 번호 자동(max+1), 긴급 체크박스.
- `updateImprovement_()` (POST) — **담당자만**(행의 담당자 == author) 상태 변경. 개선완료→완료일자 자동(상태 바뀌면 비움), 반려/보류→사유 저장(그 외 비움).

## 프런트
- 신규: `src/pages/Improve/{index,ImproveWrite,improveMeta}.tsx`, `src/store/slices/improveSlice.ts`, 타입 `ImprovementItem`, API `fetchImprovements/createImprovement/updateImprovement`.
- 사이드바 **새 "소통" 그룹 + 개선제안**(`LightbulbOutlined`), 배지=접수중 건수(`useNavBadges.improve`). 라우트 `/improve`. MainLayout 진입 시 `loadImproveData`.
- **목록형 표**: 열 `번호·유형·개선위치·제목·작성자·제안일자·담당자·상태·비고`(제목·비고만 좌측, 나머지+헤더 가운데). 최신순 정렬.
  - 제목칸: 긴급이면 맨 앞 `PriorityHigh`(빨강), 관련자료 있으면 끝에 외부링크 아이콘(없으면 미표시).
  - 행 클릭 → 개선내용 **아코디언**(헤더·화살표 없음).
  - 상태칸: 담당자에게 드롭다운(5상태), 그 외 읽기전용 칩. 색 접수중=초록·검토중=회색·개선완료=파랑·보류=주황·반려=빨강.
  - 비고칸: 개선완료→완료일자 / 반려·보류→사유(담당자는 **인라인 편집**, 상태 전환 시 Enter로 저장) / 그 외→—.
- **필터 탭**: 접수중·검토중·개선완료·반려·보류(전체 탭 없음). 기본 접수중. 같은 탭 재클릭=전체. **Shift+클릭=중복선택**. 탭 색=상태 색.
- **제안 등록**(관리자): 모달(긴급·유형·개선위치·제목·개선내용·관련자료·담당자). 작성자=로그인 이름 자동, 담당자=작성자가 선택(상태변경 권한자).

## 권한
- 작성 = 로그인 사용자. 상태변경 = 담당자(프런트 게이팅 + 백엔드 검증).

## 보완 (피드백 반영, @44)
- 상태명 변경: 개선완료→**완료**, 반려→**불가**. 최종 상태 = 접수중·검토중·완료·보류·불가. (백엔드 `updateImprovement_`도 완료/불가 기준으로 수정·재배포 @44)
- 열 순서: **번호·제목·유형·개선위치**·작성자·제안일자·담당자·상태·비고 (유형·개선위치를 제목 뒤로). 모든 헤더 가운데 정렬(제목·비고 셀만 좌측).
- 상태 드롭다운 화살표 제거(`IconComponent={()=>null}`).
- 필터탭 기본 = **아무것도 선택 안 함(전체 표시)**.
- 아코디언 **다중 펼침**(Set 기반, 누르는 것마다 독립 토글).
- 사유 인라인 편집: Enter + **저장(체크)·취소(✕) 버튼** 추가(마우스 클릭 완료 가능).
- 헤더 '제안 등록' 버튼 제거 → 표 맨 아래 **인라인 '+ 새 제안'** 행(관리자, 클릭 시 등록 모달).
- 네트워크 'Failed to fetch' 대비: 조회·상태변경에 **재시도(최대 3회)**(`withRetry`). 등록은 중복 방지 위해 단일 시도.

## 보완 2 (피드백, 백엔드 @45)
- 단어 **접수중→접수**(기본 상태값 백엔드 @45). 옛 '접수중'은 `normStatus`로 '접수' 별칭(시트 마이그레이션 불필요).
- 상태/탭 색: **접수=회색, 검토중=초록**, 완료=파랑·보류=주황·불가=빨강. 상태칩이 탭 색 규칙과 동일.
- 유형 추천 UI·UX 추가(`IMP_TYPE_OPTIONS`, 자유입력 유지, datalist).
- 열 순서 **개선위치 ↔ 유형** 교체. 열 너비: 제목=가변(100%)·나머지 최소(1%·nowrap).
- **로그인 영역을 사이드바 하단으로 이동**(`SideNav` 푸터, PC). 모바일은 TopBar 유지(`.topbar-auth` = 모바일 전용 노출).
- 비담당자 상태칩 `cursor:not-allowed`. 상태 드롭다운 화살표 제거. 필터탭 기본=전체(미선택). 아코디언 다중 펼침 + **제목 칸에 맞춰 들여쓰기**(번호 빈칸 + colSpan 8). 비고 완료=**날짜만**.
- **보류/불가 사유=팝업**(저장 초록·취소 빨강, 안내문 「제목」 ~사유를 입력해주세요, 부제 없음).
- **새 제안=인라인 작성 행**(모달 폐지): 긴급 토글(느낌표 임베드)·제목(게시글 제목칸과 좌측정렬)·개선위치·유형·담당자·관련자료(맨 아이콘)·개선내용(제목폭 동일·높이 2배)·취소/저장. `ImproveWrite.tsx` 삭제.

## 보완 3 (피드백)
- **로그인 위치 원복**: 사이드바 하단 → **상단 우측(TopBar)** 그대로(이동 취소, `.topbar-auth` 제거).
- **담당자 열 제거**(표시만; 데이터·권한·작성 시 담당자 선택은 유지). 표 8열: 번호·제목·개선위치·유형·작성자·제안일자·상태·비고.
- **새 제안 인라인을 게시글 열에 맞춰 정렬**: 긴급 토글은 제목 칸 앞, 제목/개선위치/유형 입력이 각 열 위치·너비를 그대로 사용(입력 width 100%), 작성자=로그인이름·제안일자=오늘·상태=접수 자동 표시, 취소/저장은 비고 칸(우측). 담당자 선택 + 개선내용은 둘째 줄(제목 칸 정렬).
- **아코디언 배경**: 개선내용 펼침 배경을 **제목 열까지만** 채움(번호 빈칸 + 제목 칸 배경 + 나머지 colSpan 빈칸).

## 보완 4 (피드백)
- **펼친 글 강조**: 클릭해 펼치면 그 글의 **제목 행 + 개선내용 행을 모두 같은 색(파랑 0.1)으로** 채워 다른 글과 구분(아코디언 콘텐츠는 제목 칸부터 colSpan 7 전폭).
- **긴급 토글을 제목 입력칸 내부 체크박스로**: `InputBase startAdornment`에 18px 정사각 체크박스(! 임베드) — 체크 시 빨강 채움+흰 !. 제목 입력칸은 제목 셀을 꽉 채움(셀 왼쪽부터). 관련자료는 `endAdornment`.
- **개선위치·유형**: 입력칸 너비 66px로 축소 + `ExpandMore`(▾) endAdornment(드롭다운 표시). datalist 유지.
- **담당자 행·선택 완전 삭제** → 등록 시 **담당자=작성자(등록자) 자동 지정**(권한자).
- **개선내용**: 너비를 제목~유형(colSpan 3)까지 넓힘, 높이는 취소·저장 버튼과 동일(단일행). **취소·저장은 가로 배치로 개선내용 우측(colSpan 4)**.

## 보완 5 (피드백)
- **위치·유형 = 드롭다운 버튼**(MUI Select, ▾): 클릭하면 목록 표시. 옵션은 **구글시트 데이터 확인(드롭다운) 목록**을 백엔드에서 읽어 사용. 백엔드 `getImprovements_`가 `dvOptions_`로 개선위치/유형 열의 `DataValidation`(VALUE_IN_LIST·VALUE_IN_RANGE) 추출 → `locOptions/typeOptions` 반환(@46). 프런트는 slice에 저장해 사용(없으면 기존 데이터 폴백). (위치=홈·업무일정·업무현황·장비운영관리·장비도입관리·공지사항·구축 로드맵·바로가기·설정·기타 / 유형=기능·UI·UX·콘텐츠·모바일·오탈자·기타)
- 취소·저장 버튼 **우측 정렬(flex-end)**.

## 보완 6 (피드백, 백엔드 @48)
- **취소·저장 버튼을 상태 열까지 당김**: 둘째 줄을 `번호(빈칸) + 개선내용(colSpan3: 제목·개선위치·유형) + 버튼(colSpan3: 작성자·제안일자·상태, flex-end, `pr:0`) + 비고(빈칸)`로 재배치. 버튼 셀 우측 패딩 제거로 **저장 버튼 우측 모서리 = 상태 열 우측 경계(측정 diff 0px)**. (이전엔 비고 칸까지 차지해 표 맨 오른쪽에 붙었음.)
- **웹 작성/수정 행도 시트에서 드롭다운(칩)으로 저장**: `appendRow`/`setValue`는 열의 데이터 확인(Data Validation)을 새 행에 복사하지 않아, 웹에서 만든 행은 상태·담당자·유형·개선위치가 **평문**으로 들어갔다. 백엔드에 `copyRowValidation_(sh, hIdx, targetRow, probeCol)` 추가 — 검증이 살아있는 템플릿 행(상태 열 기준 스캔, 폴백=첫 데이터행)에서 `range.copyTo(dst, PASTE_DATA_VALIDATION)`로 한 행 전체의 데이터 확인을 복사. `createImprovement_`(append 직후, 긴급 체크박스 적용 전)와 `updateImprovement_`(상태 변경 후)에서 호출 → 신규 행은 물론, 드롭다운 없던 기존 웹 작성 행도 상태 변경 시 드롭다운이 입혀진다. 백엔드 @48(URL 불변).
  - ⚠ 참고: 새 구글시트 드롭다운은 칩 **색상**도 데이터 확인 규칙에 포함돼 함께 복사된다. 만약 색이 **조건부 서식**으로 칠해진 경우엔 복사 대상이 아니므로, 그 색은 시트에서 해당 열 전체로 조건부 서식 범위를 넓혀야 신규 행에도 적용된다(드롭다운 자체는 정상 적용).

## 보완 7 (피드백, 백엔드 @51)
- **취소·저장 버튼을 제안일자 열 가운데로**: 둘째 줄 버튼 박스 `justifyContent: flex-end → center`, 셀 `pr:0` 제거. 버튼 그룹 중심이 제안일자 열 중심과 정렬(실측 버튼중심 636 vs 제안일자중심 634, 오차 2px). 상태열 우측에 붙어 있던 걸 왼쪽(가운데)으로 이동.
- **시트 드롭다운 미반영 원인 규명·복구**: 임시 진단 엔드포인트(`debugImproveDV`)로 실제 데이터 확인 상태를 점검. 결과 — `copyTo(PASTE_DATA_VALIDATION)`는 **정상 동작**(복사 시 VALUE_IN_LIST 적용 확인), @48 이후 생성된 행(예: row 9)은 드롭다운이 잘 붙음. 다만 **@48 이전 생성된 옛 행(row 8)은 데이터 확인이 없는 평문** 상태로 남아 있었음 → 이게 "아직 안 됨"의 원인.
  - 일괄 복구 엔드포인트(`backfillImproveDV`)를 추가·1회 실행 → 데이터 확인이 없던 기존 행 전부에 템플릿 행의 데이터 확인 복사(개선위치/유형/담당자/상태 모두 VALUE_IN_LIST로). 진단으로 row 4~9 전부 적용 확인.
  - 복구 완료 후 **임시 진단/복구 엔드포인트는 제거**(보안·정리). 영구 수정(`copyRowValidation_` 호출 in create/update)은 유지. 백엔드 @51.

## 보완 8 (피드백, 백엔드 @52)
- **보류·불가 사유 저장/표시 버그 수정(원인=헤더명 불일치)**: 시트의 사유 열 헤더는 `사유 (보류/불가)`인데 백엔드 `improveCtx_`의 `reason` 매칭이 정확문자열(`사유`·`불가사유`…)만 봐서 `C.reason=-1`로 잡혔다 → 사유가 **시트에 저장되지 않고**(set은 i<0이면 no-op), 읽기도 항상 빈 문자열이라 비고칸이 무조건 '사유 입력'으로 보였다. `improveCtx_`에 **'사유' 포함 헤더 폴백**(`reasonCol()`: 정확매칭 실패 시 헤더에 '사유'가 들어간 첫 열) 추가. 백엔드 @52(URL 불변). (라이브 API 확인: 헤더 `…,상태,완료일자,"사유 (보류/불가)"`, 기존 보류·불가 행 reason 전부 "" 였음 → 수정 후 신규 저장부터 반영.)
- **정렬 = 번호 내림차순**: `listed` 정렬을 제안일자 우선 → **번호(높은→낮은) 우선**(동률 시 제안일자 최신)으로 변경.
- **0건 상태 탭 숨김**: 필터 탭을 `visibleStatuses`(건수>0 또는 현재 선택된 상태만)로 렌더. 선택 중이던 탭이 0건이 돼도 토글 가능하도록 유지. (예: 검토중 0건 → 탭 미표시)
- **Shift 다중선택 안내문**: 탭 우측에 `Shift+클릭으로 여러 상태 선택`(text.disabled, 11.5px). 탭이 2개 이상일 때만 노출.
- **새 제안 = (원복) 표 안 인라인 작성행 유지**: 사용자의 6번 항목은 "구현 가능한지?"라는 **질문**이었는데 구현까지 진행해 되돌림(원복 커밋). 표 바깥 우상단 버튼 + 좌측 펼침 애니메이션 시도는 제거하고, 기존 방식(표 맨 아래 점선 `+ 새 제안` 행 → 클릭 시 표 안 인라인 작성 2행)을 그대로 유지. `index.tsx`는 직전 커밋(72d2400) 버전으로 복원 후 위 ①정렬·②0건탭·③Shift안내만 재적용.
  - 참고(가능성 검토 결과): 좌측 펼침 애니메이션은 **구현 가능**. 다만 이 레이아웃에선 함정이 있다 — ① MUI `Collapse orientation="horizontal"`은 entered 폭이 0으로 잡혀 부적합, ② **flex 항목+overflow:hidden은 폭 0 붕괴**(text-align 우측정렬+inline-block로 회피), ③ sx `maxWidth`가 0px로 오계산돼 inline `style` 사용, ④ **headless dev 프리뷰는 CSS transition 중간 프레임을 못 그림** → 최종상태로만 검증 가능. (추후 사용자가 요청하면 재적용.)
- 검증: type-check·build 통과. 라이브 dev에서 ①정렬(번호 7→1) ②검토중(0건) 탭 숨김 ③Shift 안내문 ④표 안 인라인 작성행 정상 동작(점선행 클릭→제목/저장 노출) 확인.

## 보완 9 (피드백, 백엔드 @53)
- **새 제안 버튼 위치 이동 + dashed 박스**: 표 안 점선행/인라인 작성행을 들어내고, **필터 헤더와 목록(표) 사이**에 독립 영역 배치. 평소엔 **`+ 새 제안` dashed 박스 버튼**(`1.5px dashed` 테두리·라운드·hover 시 초록), 클릭하면 같은 자리에 작성 카드(`ComposeCard`)가 펼쳐진다.
- **작성 카드 접기**: `ComposeCard`의 **제목줄 배경 클릭 시 접힘**(`onCancel`) + 제목줄 우측 `ExpandLess(▲)` 접기 버튼. 입력 요소(긴급 체크박스·제목 InputBase)는 `stopPropagation`이라 클릭해도 안 접힘. (이전엔 취소 버튼으로만 닫혔음)
- **글마다 수정/삭제 버튼**: 아코디언을 펼치면 **내용 줄 우측(비고열 쪽)**에 `수정`(EditIcon)·`삭제`(DeleteOutlined) 버튼 노출(담당자=`canManage`만). 
  - **수정** = 아코디언 자리 **in-place 편집**(`ComposeCard editing`, 기존 값 프리필) → 저장 시 `updateImprovement`로 제목/내용/유형/위치/관련자료/긴급 갱신(상태·완료일자·사유는 보존). 
  - **삭제** = 확인 팝업(「제목」 삭제할까요?) → `deleteImprovement`.
- 공용 컴포넌트 `ComposeCard`(자체 상태) 신설 — 새 제안(상단)·수정(아코디언 in-place) 양쪽 재사용.
- **백엔드 @53**: `updateImprovement_`를 내용 필드(title/content/type/loc/link/urgent) 수정 지원으로 확장(**status 미전달 시 완료일자·사유 보존** — 상태 로직은 `req.status!==undefined`일 때만), `deleteImprovement_` 신규(담당자 검증 후 행 삭제) + `doPost` 라우팅. API `updateImprovement` 페이로드 확장 + `deleteImprovement` 추가.
- 권한: 수정/삭제 모두 **담당자(행 mgr==author)만**(프런트 `canManage` 게이팅 + 백엔드 검증). 신규 등록 시 mgr=작성자라 작성자 본인이 관리.
- 검증: type-check·build 통과. 라이브 dev(관리자)에서 ①dashed 박스가 헤더-표 사이 위치 ②클릭→작성카드, 제목줄 배경 클릭→접힘(입력 클릭은 유지) ③글 펼침→우측 수정/삭제, 수정→프리필 in-place 카드(저장버튼 '수정'), 삭제→확인 팝업(「제목」) 모두 확인. (실제 저장/삭제는 담당자 로그인 필요 — 사용자 확인 권장)

## 보완 10 (피드백 — 보완 9 위치/구조 정정, 프런트 전용)
- **새 제안 위치 정정**: "헤더와 목록 사이"를 표 바깥(필터탭 아래)으로 해석했던 걸, 사용자 의도대로 **표 내부 최상단(컬럼 헤더 바로 아래·최신글 위)** 행으로 이동. `+ 새 제안`은 표 첫 tbody 행이며 **텍스트에 dashed 박스**(inline-flex, `1.5px dashed`, hover 초록).
- **작성 입력 구조 원복**: 보완 9에서 만든 stacked 카드(`ComposeCard`)를 폐기하고, **이전 버전과 동일한 열 정렬 인라인 2행 구조**로 복구 — 1행: 번호(빈/번호)·제목 InputBase(긴급 체크박스 startAdornment + 관련자료 LinkField endAdornment, width 100%)·개선위치 DropField(96)·유형 DropField(84)·작성자·제안일자·상태칩, 2행: 개선내용 InputBase(colSpan3, height 32) + 취소/저장(colSpan3 가운데). 
- **신규/수정 공용 `renderCompose(mode, t?)`**: 새 제안은 표 최상단, **수정은 해당 글 자리 in-place**(같은 열 정렬, 번호·작성자·제안일자·현재 상태칩 표시, 저장 버튼 '수정'). 상태/완료일자/사유는 수정에서 건드리지 않음.
- **제목줄 접기 유지**: 1행 `TableRow onClick=접기`, 입력이 든 제목/위치/유형 셀만 `stopPropagation` → 배경(번호·작성자·제안일자·상태 셀) 클릭 시 접힘. (취소 버튼도 가능)
- 수정/삭제 버튼·삭제 확인 팝업·백엔드(@53)는 보완 9 그대로. `ComposeCard`·`ExpandLess` import 제거, c* 입력 상태 복구.
- 검증: type-check·build 통과. 라이브 dev에서 ①`+ 새 제안`이 표 첫 행(헤더 아래) ②클릭→열 정렬 작성행(긴급 in 제목·DropField in 위치/유형·작성자/오늘/접수칩) ③제목 클릭은 유지·작성자칸 클릭은 접힘 ④수정→in-place 프리필(번호 7·제목/내용 복원·저장'수정') ⑤삭제→「제목」 확인 팝업 모두 확인.

## 보완 11 (피드백 — 시안 확정 후 구현, 프런트 전용 / 시각 시안 다회 반복 후 "시안 A" 채택)
- **리네이밍**: 페이지 제목·사이드 메뉴 라벨 `개선제안 → 포털개선요청`, 새 글 버튼 `새 제안 → 새 요청`, 등록 스낵바·삭제 다이얼로그·빈 상태 문구도 "요청"으로 통일. 라우트 `/improve`·시트/백엔드 무변경(@53).
- **정렬 변경**: 1순위 상태(접수→검토중→보류→완료→불가, `IMP_STATUSES` 순서 + `statusRank`), 2순위 제안일자 최신순, 동률 시 번호 내림차순. 필터 탭·상태 드롭다운 순서도 동일. (이전: 번호 내림차순) — `improveMeta.ts`에 `IMP_SETTLED`·`isSettled`·`statusRank` 추가.
- **새 요청 버튼 이동**: 표 안 dashed 행을 들어내고 **헤더 우상단 버튼**(MUI `Button` outlined)으로. 기본=초록(accent.green) 글씨 윤곽, 호버=`darken(accent.green, .22)` 채움+흰 글씨. 클릭 시 작성 행은 표 최상단(헤더 아래·최신글 위)에 그대로 열림(`composing && renderCompose('new')`).
- **작성/수정 행 개편**: 긴급(!) 체크박스를 제목칸 밖 **번호열**로 이동(startAdornment 제거), 제목칸은 관련자료 링크 아이콘만, **개선내용 멀티라인**(`multiline`·Enter/Shift+Enter 줄바꿈·세로 자동확장)을 **제목~상태열(colSpan6)** 한 박스로, 취소/저장을 **저장(Check)·취소(Close) 아이콘 8px 간격**으로 비고열에 배치(텍스트 버튼 폐지).
- **목록 행 상태 시각화**:
  - 전 행 배경 = 상태색 옅은 틴트 `alpha(kindColor(th,kind), 0.07)`.
  - **완료·보류·불가(종결)** = 상태칩 제외 모든 글자(번호 포함) **60% 흐림 + 상태색**(시안 A). 접힌 상태에서만 상태색 적용, 펼치면 가독성 위해 색은 빼고 흐림만 유지(`txtColor = settled && !open ? kindColor : normal`).
  - 펼친 글: 제목줄 진한 파랑 `alpha(blue,0.22)` / 내용줄 옅은 파랑 `alpha(blue,0.09)`, **내용줄 배경 클릭도 접힘**(content row `onClick=toggle`).
  - 수정·삭제 = 텍스트 제거, **아이콘 전용 IconButton**(Edit·DeleteOutlined, Tooltip).
- **배지 보정**: `useNavBadges.improve`가 옛 `접수중`만 세던 것을 `접수`(+레거시 `접수중`) 집계로 수정.
- 색 규칙 준수: 신규 색은 모두 `tokens.ts`/`accent.*`·`kindColor`·`darken`/`alpha`로 파생(하드코딩 없음).
- 검증: `npm run type-check` 통과. **로컬 dev는 Apps Script 백엔드 네트워크 차단으로 '불러오기 실패'** → 사이드 메뉴/제목/빈 상태 문구·콘솔0만 확인(데이터 행 틴트·흐림·정렬·작성/수정/삭제 시각은 라이브 검증 필요). 스크린샷 도구는 본 환경에서 타임아웃(스냅샷은 정상).
- 변경 파일: `src/pages/Improve/index.tsx`, `src/pages/Improve/improveMeta.ts`, `src/layouts/SideNav.tsx`, `src/layouts/useNavBadges.ts`.
- **시트 탭 리네임 대응 (백엔드 @54)**: 배포 직후 사용자가 구글시트 탭을 `개선사항 → 포털개선요청`으로 변경 → 백엔드가 `개선사항`만 찾아 `'개선사항' 시트가 없습니다` 실패(불러오기 실패). `improveCtx_`를 **여러 후보 이름(`포털개선요청`·`개선사항`·`개선제안`) 순차 탐색**으로 보강(`IMPROVE_SHEET_NAMES`, 헤더 미탐지 에러도 `sh.getName()`로). clasp `redeploy @54`(URL 불변). 추가로 프런트 `fetchImprovements`에 **캐시 우회 파라미터(`&_=Date.now()`)** — Apps Script GET 응답 캐시로 옛 에러/데이터가 남는 것 방지. 라이브 dev 검증: 보드 정상 로딩(접수2·보류2·완료2·불가1, 상태→최신순, 배지 '포털개선요청 2', 콘솔0).

## 보완 12 (피드백 — 종결행 강조 원복·정렬 변경·버튼 스타일, 프런트 전용)
- **완료·보류·불가 글자 강조 원복**: 보완 11에서 넣은 종결행 글자 상태색 + 60% 흐림을 제거 → 모든 상태에서 번호·제목·개선위치·유형·작성자·제안일자·비고를 원래 색(번호/제안일자=secondary, 제목/위치/유형/작성자=primary, 비고 완료일자=info, 사유=secondary)으로 복구. `settled`/`fade`/`txtColor` 로직 삭제(+`isSettled` import 제거). **행 배경 상태색 틴트와 펼친 글 진한/옅은 파랑은 유지.**
- **정렬 변경**: 1순위 **번호 내림차순**(높은 번호=최신 위), 2순위 상태(`statusRank`). 번호가 유일하므로 사실상 번호순. 보완 11의 상태 우선 정렬 폐지(`dateSortValue` 미사용 → import 제거).
- **새 요청 버튼 = 필터 탭 칩 스타일**: 기본=초록 글씨 + `alpha(green,0.12)` 배경 + `alpha(green,0.32)` 테두리, hover=`alpha(green,0.2)`, **클릭(`:active`)=초록 채움 + 흰 글씨**(선택된 칩 모양). `darken` import 제거.
- 검증: type-check 통과, 라이브 dev에서 번호 내림차순(7→1)·데이터 정상·콘솔0. (참고: 번호 정렬은 내림차순 채택 — 오름차순 원하면 한 줄 수정.)

## 보완 13 (피드백 — 작성행 치수·정렬·버튼 모션, 프런트 전용)
- **제목 입력칸 = 개선위치/유형 드롭다운 높이 통일**: 둘 다 `height: 32`(px) 명시 → 동일 높이(이전엔 `inputSx` 패딩 기반 자동높이라 미세 차이 가능).
- **개선내용 입력칸 세로 가운데 정렬**: 둘째 줄 `<TableRow>`의 td `verticalAlign: top → middle`, 개선내용 InputBase `alignItems: flex-start → center` + `minHeight: 32`(빈 칸도 32px, 줄 늘면 아래로 확장).
- **긴급 체크박스 18→24px**: `width/height: 24`, `!` 글자 12→15px, 모서리 4→5px.
- **새 요청 버튼 클릭 시 부드러운 채움**: 채움 상태를 `composing`에 연동 + `transition .2s` → 클릭해 작성칸이 열리면 초록 바탕·흰 글자로 스르륵 전환, 닫으면 원복(기존 `:active` 순간 전환 폐지).
- 검증: type-check 통과, 라이브 dev 목록 정상 로딩·콘솔0. (작성행·새 요청 버튼은 관리자 전용이라 치수/모션 실측은 관리자 로그인 후 확인 권장.)

## 보완 14 (피드백 — 드롭다운 글자 가운데·개선내용 가운데·새 요청 토글·필터탭 호버 채움, 프런트 전용)
- **위치/유형 드롭다운 글자 가운데 정렬**: `.MuiSelect-select`에 `justifyContent: center` + 좌우 패딩 `pl/pr 20px` 대칭(화살표 공간 보정).
- **개선내용 입력칸 세로 가운데(+제목칸 높이 맞춤)**: 보완 13의 `minHeight`/`alignItems` 방식이 멀티라인 textarea에선 위쪽 정렬로 보여, **세로 패딩 `py: 7px`(상·하 동일)로 단일 줄을 중앙 배치** → resting 높이 ≈ 제목칸 32px, 텍스트 중앙. 줄 늘면 아래로 확장 유지.
- **새 요청 버튼 토글**: 다시 누르면 작성칸이 닫히도록 `onClick`을 `composing ? setComposing(false) : openNew()`로. 버튼 채움도 `composing` 연동이라 열림/닫힘에 맞춰 전환.
- **필터 탭 호버 채움**: 탭 칩에 `transition .2s` + **hover 시 상태색 채움 + 흰 글자**(바탕·글자색 동시 전환), 선택 시 유지. (이전엔 hover가 배경만 살짝 진해짐.)
- 검증: type-check 통과, 라이브 dev 목록·필터탭 정상(접수 칩 기본 옅은 틴트·높이 32px 확인)·콘솔0. (작성행·버튼 실측은 관리자 로그인 후.)
